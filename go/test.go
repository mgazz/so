package main

import (
	"log"
)
func main() {
		
	var libera [10]bool
	var sli[10]bool

	// inizializzazioni:
	for i := 0; i < 10; i++ {
		libera[i] = true
	}
	
	for i:= range sli {
		sli[i] = true
	}
	log.Println(libera)
	log.Println(sli)


}
